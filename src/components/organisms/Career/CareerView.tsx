import classNames from "classnames";

const style = {
  Container: classNames("flex h-screen"),
};

const CareerView = () => {
  return <div className={style.Container}>Career</div>;
};

export default CareerView;
